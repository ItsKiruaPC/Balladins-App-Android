<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Ex6WebService" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/my_dark_primary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.Ex6WebService" parent="Base.Theme.Ex6WebService">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundColor">@color/my_dark_primary</item>
        <item name="itemBackground">@color/my_dark_primary</item>
    </style>

    <style name="Theme.Ex6WebService1" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>